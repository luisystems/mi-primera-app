import React from "react";

// export const Title = () => <h1>Hola Mundo</h1>;
// export const Title = (props) => <h1>{props.value}</h1>;

/**
 * TIPO DE COMPONENTE
 * - Presentacion
 * - Stateless
 */

export const Title = ({ value }) => <h1>{value}</h1>;
